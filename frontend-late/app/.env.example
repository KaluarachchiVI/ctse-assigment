# Set to `true` to use the demo mock payment page (`/payment`) after reserving seats.
# Omit or set to anything else to keep the default Stripe Checkout redirect flow.
# Pair with payment-service `PAYMENT_GATEWAY_PROVIDER=mock` for synchronous success without Stripe.
NEXT_PUBLIC_USE_MOCK_PAYMENT_PAGE=
